# DESIGN.md 模板

> 根据输入（.pptx / 截图 / 网站 / 关键词）提取并填写。所有带 `[待填]` 的字段必须替换为实际值，不保留占位符。

---

## 基础信息

- **项目名称**：[待填]
- **主题风格**：[待填，如：暗色科技、极简白、渐变赛博]
- **整体色调**：[暗色 / 亮色 / 混合交替]
- **幻灯片尺寸**：1440×900px（16:9）

---

## 颜色系统

```css
:root {
  /* 背景 */
  --bg-primary:   [待填];   /* 主背景色，如 #000000 或 #FFFFFF */
  --bg-secondary: [待填];   /* 次级背景/卡片背景 */

  /* 文字 */
  --text-primary:   [待填]; /* 主要文字色 */
  --text-secondary: [待填]; /* 次要文字色（副标题/说明文字） */

  /* 强调 */
  --accent:       [待填];   /* 主强调色（按钮/高亮/数字） */
  --accent-light: [待填];   /* 强调色浅色变体（背景/标签） */

  /* 边框/分隔 */
  --border:       [待填];   /* 卡片边框、分隔线 */
}
```

---

## 字体系统

> 从输入来源提取实际字体名，中文字体作为 fallback 追加在末尾，不写死。

```css
:root {
  --font-heading: '[待填]', 'PingFang SC', 'Helvetica Neue', sans-serif;
  --font-body:    '[待填]', 'PingFang SC', 'Helvetica Neue', sans-serif;
}
```

| 用途 | 字体名 | 字重 | 字号范围 |
|------|--------|------|----------|
| 主标题 | [待填] | [待填] | [待填，如 80-120px] |
| 副标题 | [待填] | [待填] | [待填，如 20-28px] |
| 正文/说明 | [待填] | [待填] | [待填，如 16-18px] |
| 标签/小字 | [待填] | [待填] | [待填，如 11-13px] |
| 装饰数字 | [待填] | [待填] | [待填，如 150-200px] |

---

## 布局规范

- **页面内边距**：[待填，如 80px 60px]
- **内容最大宽度**：[待填，如 1200px 或 100%]
- **网格列数**：[待填，如 3列 / 4列 / 自由布局]
- **卡片间距（gap）**：[待填，如 24px]
- **圆角**：[待填，如 12px / 20px / 0]

---

## 组件风格

### 卡片
```css
/* [待填，描述背景/边框/投影风格] */
background: ;
border: ;
border-radius: ;
box-shadow: ;
```

### 标签 / 徽章
```css
/* [待填] */
font-size: ;
letter-spacing: ;
border-radius: ;
background: ;
color: ;
```

### 按钮 / CTA
```css
/* [待填] */
background: ;
color: ;
border-radius: ;
padding: ;
```

### 分隔线
```css
/* [待填，如渐变线/纯色线/无] */
```

### 背景装饰
```css
/* [待填，如光晕/网格/大字/无] */
/* 示例：径向渐变光晕 */
/* background: radial-gradient(circle at 30% 50%, rgba(主色, .15), transparent 60%); */
```

---

## Section 清单

列出每个幻灯片页面的 CSS class 和内容主题：

| # | CSS Class | 内容主题 | 背景色调 |
|---|-----------|----------|----------|
| 1 | [待填] | [待填] | 暗/亮 |
| 2 | [待填] | [待填] | 暗/亮 |
| … | … | … | … |
